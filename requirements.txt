# StrangeUtaGame — 运行时依赖锁定（跨平台）
#
# pip install -r requirements.txt
#
# 平台特定依赖通过 PEP 508 环境标记自动选择：
#   Windows: 安装 winrt-* (WinRT IME 主引擎)
#   Linux/macOS: 安装 fugashi + unidic-lite (MeCab 跨平台回退)
#
# 如需开发/打包工具，额外安装：
#   pip install -e ".[dev,build]"    # 从 pyproject.toml 安装
#   pip install -r requirements-dev.txt  # 或使用锁定版本

# ── 跨平台核心依赖 ──
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6_sip==13.11.1
PyQt6-Fluent-Widgets==1.11.2
sounddevice==0.5.5
soundfile==0.13.1
pedalboard==0.9.22
numpy==2.4.4
pykakasi==2.3.0
jaconv==0.5.0
pyphen==0.17.2
requests==2.32.5
urllib3==2.6.1
charset-normalizer==3.4.4
certifi==2025.11.12
idna==3.11

# ── Windows: WinRT IME（日语注音主引擎） ──
# Windows.Globalization.JapanesePhoneticAnalyzer
# 需要系统日语 IME 功能 Language.Basic~~~ja-JP
winrt-Windows.Globalization==3.2.1 ; platform_system == "Windows"
winrt-Windows.Foundation==3.2.1 ; platform_system == "Windows"
winrt-Windows.Foundation.Collections==3.2.1 ; platform_system == "Windows"

# ── Unix/Linux: fugashi (MeCab) 跨平台回退注音引擎 ──
# 非 Windows 系统自动安装，提供形态素级别的日语注音（优于 pykakasi 单字级）
fugashi>=1.3.0 ; platform_system != "Windows"
unidic-lite>=1.0.0 ; platform_system != "Windows"
