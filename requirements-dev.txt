# StrangeUtaGame — 开发/打包工具依赖（锁定版本）
#
# 方式一（推荐，从 pyproject.toml 安装）：
#   pip install -e ".[dev,build]"
#
# 方式二（使用锁定版本）：
#   pip install -r requirements-dev.txt
#   pip install -e .

# ── 开发工具 ──
pytest==9.0.3
pytest-cov==7.1.0
pytest-qt==4.5.0
black==26.3.1
ruff==0.15.10
mypy==1.20.1
psutil>=5.9.0

# ── 打包工具 ──
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.4
